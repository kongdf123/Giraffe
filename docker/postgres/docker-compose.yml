services:
  postgres:
    image: postgres:16
    container_name: aiflow-postgres
    environment:
      POSTGRES_DB: aiflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
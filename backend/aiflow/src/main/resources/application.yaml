server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aiflow
    username: postgres
    password: postgres

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl